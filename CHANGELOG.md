# 更新记录

## v1.1

- 修复深层目录（例如微信下载文件夹）导致 PyTorch 长文件名安装失败的问题。
- 将运行环境固定放在当前用户 `%LOCALAPPDATA%\DV\r1`，避免修改系统 Python、PATH 或注册表。
- 安装失败提示不再统一归因于网络。
- 提供带模型的 Windows x64 便携包、轻量包和 SHA256 校验文件。
- 补充快速上手、下载说明、第三方许可和自动发布流程。

## v1.0

- 本机全画面相对深度转换，支持快速约 15 fps 和原帧率两种模式。
- 原视频与结果预览、进度、取消、结果文件夹和日志入口。
- 首次自动安装独立 CPU 推理环境，后续支持离线处理。

## 已知限制

首次下载没有精确百分比；日志按时间累积。失败或取消后可能留下中间文件。当前桌面版使用 CPU，长视频的内存需求较高；默认上限为 1800 个推理帧。深度结果不是精确距离，推理缩小画面后再恢复输出尺寸，细节可能损失。
